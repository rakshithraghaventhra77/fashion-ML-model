services:

  ml-service:
    build: ./ml-service
    container_name: ml-service
    ports:
      - "8000:8000"

  api-gateway:
    build: ./api-gateway
    container_name: api-gateway
    ports:
      - "8080:8080"
    depends_on:
      - ml-service
